interface:
  display_name: "Collect LDM Data"
  short_description: "Collect, augment, and validate LDM training data"
  default_prompt: "Use $collect-ldm-data to collect an LDM task run into validated ldm-2.0 and reasoning-augmented SFT data."
