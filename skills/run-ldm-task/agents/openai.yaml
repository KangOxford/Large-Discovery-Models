interface:
  display_name: "Run LDM Task"
  short_description: "Validate and run an existing LDM task safely"
  default_prompt: "Use $run-ldm-task to validate and run an existing LDM task from its repository config."
