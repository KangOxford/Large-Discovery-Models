diff a/AntBO/bo/main.py b/AntBO/bo/main.py	(rejected hunks)
@@ -96,6 +98,34 @@ class BOExperiments:
 
         self.nb_aas = len(AA)
         self.n_categories = np.array([self.nb_aas] * self.config['seq_len'])
+        self.antigen_context = None
+        if self.config.get('llm_antigen_context', False):
+            self.antigen_context = collect_absolut_antigen_context(
+                bbox_config=self.config['bbox'],
+                timeout_s=int(self.config.get('llm_antigen_context_timeout_s', 30)),
+                include_raw=bool(self.config.get('llm_antigen_context_include_raw', False)),
+            )
+            antigen_context_path = os.path.join(self.path, 'llm_antigen_context.json')
+            save_json(self.antigen_context, antigen_context_path)
+            print(f"Saved LLM antigen context in {antigen_context_path}")
+            if self.config.get('llm_policy_command') and self.config.get('llm_generate_initial_policy', False):
+                prompt_dir = os.path.join(self.path, 'llm_prompt_context')
+                prompt_path = save_llm_context_snapshot(
+                    out_dir=prompt_dir,
+                    antigen_context=self.antigen_context,
+                    top_k=int(self.config.get('llm_prompt_top_k', 10)),
+                )
+                policy_path = os.path.join(self.path, 'llm_policy_initial.json')
+                run_llm_policy_command(
+                    command_template=self.config['llm_policy_command'],
+                    prompt_path=prompt_path,
+                    output_path=policy_path,
+                    timeout_s=int(self.config.get('llm_policy_timeout_s', 120)),
+                )
+                self.config['llm_init_policy_path'] = self.config.get('llm_init_policy_path') or policy_path
+                self.config['llm_trust_region_policy_path'] = self.config.get(
+                    'llm_trust_region_policy_path') or policy_path
+                print(f"Generated initial LLM policy in {policy_path}")
         if self.config.get('llm_ranked_init', False):
             n_candidates = int(self.config.get('llm_init_n_candidates', max(100 * self.config['n_init'], 1000)))
             print(
