# Versioned Resources

`upstream_contract.json` pins the MLS-Bench source commit, task paths, evaluator
limits, and critical source digests. `qualification_evidence.json` records only
clean-checkout evidence and must not cite generated campaign directories.
