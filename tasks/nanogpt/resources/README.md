# nanoGPT Resources

`schemas/` contains structured edit spaces. `train/` contains deterministic
mock and real seed programs copied into search states. Generated search states
and summaries belong in `../runs/` and are ignored by Git.
