name: ai4bio_mutation_effect_prediction_mock
task: ai4bio_mutation_effect_prediction
algorithm: gp_ucb
mode: mock
description: Deterministic service-free GP-UCB LDMEngine qualification run.
args:
  mock: true
  iterations: 1
  reservoir-size: 4
  evaluations-per-round: 1
  proposal-mode: deterministic
  acquisition-beta: 1.0
  out-dir: runs
  run-name: mock
